{
  "name": "dev-containers",
  "type": "module",
  "private": true,
  "scripts": {
    "format": "bunx prettier --write \"src/**/.devcontainer/devcontainer.json\"",
    "format:check": "bunx prettier --check \"src/**/.devcontainer/devcontainer.json\"",
    "test": "bun run format:check && bun run ./scripts/test-template.mjs",
    "purge": "bun run ./scripts/test-purge.mjs"
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
